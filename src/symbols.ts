/**
 * ## FQN Symbol
 *
 * Symbol used to store the FQN of classes
 */
export const FQN = Symbol.for('@cashfarm/lang.FQN');

/**
 * ## SERIALIZE_MAP Symbol
 *
 * Used by {@link ExtendedObject} to store the serialization map
 */
export const SERIALIZE_MAP = Symbol.for('@cashfarm/lang.SERIALIZE_MAP');
